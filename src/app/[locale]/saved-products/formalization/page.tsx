import FormalizationCard from '@/widgets/saved/ui/formalizationCard';
import React from 'react';

const Formalization = () => {
  return (
    <div className="custom-container">
      <FormalizationCard />
    </div>
  );
};

export default Formalization;
